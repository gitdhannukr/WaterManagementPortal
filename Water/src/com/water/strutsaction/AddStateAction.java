/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.water.strutsaction;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

import com.water.delegate.RegionalInfoDelegate;
import com.water.exception.ConnectionException;
import com.water.formbean.RegionalFormBean;
import com.water.pojo.State;
import com.water.util.UtilConstants;

/**
 * MyEclipse Struts Creation date: 09-14-2012
 * 
 * XDoclet definition:
 * 
 * @struts.action path="/addStateAction" name="RegionalFormBean" scope="request"
 *                validate="true"
 * @struts.action-forward name="failure" path="/AddState.jsp"
 * @struts.action-forward name="success" path="/AddState.jsp"
 */
public class AddStateAction extends Action {
	/*
	 * Generated Methods
	 */

	/**
	 * Method execute
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		boolean flag = false;
		RegionalFormBean regionalFormBean = (RegionalFormBean) form;// TODO
		State state = new State(regionalFormBean);
		try {
			flag = new RegionalInfoDelegate().addState(state);
			request.setAttribute(UtilConstants._STATUS,
					UtilConstants._ADD_STATE);
			return mapping.findForward("success");
		} catch (ConnectionException e) {
			request.setAttribute(UtilConstants._STATUS, e.getMessage());
			return mapping.findForward("success");
		} catch (Exception e) {
			request.setAttribute(UtilConstants._STATUS, UtilConstants.__ADD_STATE_FAIL);
			return mapping.findForward("success");
		}

	}
}