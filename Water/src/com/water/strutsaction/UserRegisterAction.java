/*
 * Generated by MyEclipse Struts
 * Template path: templates/java/JavaClass.vtl
 */
package com.water.strutsaction;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.apache.struts.action.Action;
import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;
import org.hibernate.Session;

import com.water.delegate.RegionalInfoDelegate;
import com.water.delegate.SecurityUserDelegate;
import com.water.exception.ConnectionException;
import com.water.formbean.ProfileFormBean;
import com.water.formbean.RegionalFormBean;
import com.water.pojo.Addresses;
import com.water.pojo.City;
import com.water.pojo.LoginDetails;
import com.water.pojo.UserDetails;
import com.water.util.UtilConstants;

/**
 * MyEclipse Struts Creation date: 09-20-2012
 * 
 * XDoclet definition:
 * 
 * @struts.action path="/UserRegisterAction" name="profileFormBean"
 *                scope="request" validate="true"
 * @struts.action-forward name="success" path="/UserRegistrationForm.jsp"
 */
public class UserRegisterAction extends Action {
	/*
	 * Generated Methods
	 */

	/**
	 * Method execute
	 * 
	 * @param mapping
	 * @param form
	 * @param request
	 * @param response
	 * @return ActionForward
	 */
	public ActionForward execute(ActionMapping mapping, ActionForm form,
			HttpServletRequest request, HttpServletResponse response) {
		boolean flag = false;
		RegionalFormBean regionalFormBean = (RegionalFormBean) form;// TODO
		ProfileFormBean profileFormBean = (ProfileFormBean) request
				.getSession().getAttribute("profileFormBean");
		UserDetails userDetails = new UserDetails(profileFormBean);
		userDetails.setAddresses(new Addresses(regionalFormBean));
		userDetails.setLoginDetails(new LoginDetails(profileFormBean));
		
		try {
			flag = new SecurityUserDelegate().userRegistration(userDetails);
			request.setAttribute(UtilConstants._STATUS,
					UtilConstants._REGISTER_SUCCESS);
			request.getSession().setAttribute("profileFormBean", null);
			return mapping.findForward(UtilConstants._SUCCESS);
		} catch (ConnectionException e) {
			request.setAttribute(UtilConstants._STATUS, e.getMessage());
			return mapping.findForward(UtilConstants._SUCCESS);
		} catch (Exception e) {
			System.out.println(e);
			request.setAttribute(UtilConstants._STATUS,
					UtilConstants._REGISTER_FAIL);
			return mapping.findForward(UtilConstants._SUCCESS);
		}

	}
}